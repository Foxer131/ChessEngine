// Forces chess/move.hpp to be compiled so its (uncommented) static_assert tests
// are checked when you build chess_core. No runtime role; delete once a real
// source includes the header.
#include "chess/move.hpp"
